# Main Resume Source Directory

This directory contains the **source files** used to generate the final resume PDF.  
Nothing in this folder depends on OS tools, except when compiling TeX to PDF.

## 📁 Structure

```

/main
│
├── contents.json      # Resume data (source of truth)
├── layout.tex         # LaTeX template (design + placeholder variables)
├── main.tex           # Generated file (DO NOT EDIT)
│
└── scripts/           # Build scripts
├── compile_tex.py
├── build_pdf.sh
└── build.sh

````

---

## 🧱 File Responsibilities

### `contents.json`
All resume content lives here.  
Edit **only** this file to update your resume text, projects, experience, skills, etc.

### `layout.tex`
Defines the LaTeX template and layout.  
Edit this file if you want to change styling, formatting, fonts, or section structure.

### `main.tex`  
🚫 **Do not edit this file manually.**  
This file is generated automatically from the JSON + layout.

It should be added to `.gitignore`.

---

## 🛠 How to build (local)

From **repo root (`/resume`)**:

```bash
./main/scripts/build.sh
````

or if you have `build_resume.sh`:

```bash
./build_resume.sh
```

This will:

1. Convert `contents.json` + `layout.tex` → `main.tex`
2. Compile `main.tex` → `public/RESUME_ShivalGupta.pdf`
3. Copy profile photo (if configured)

---

## ⚙️ Requirements (for local builds)

You need:

* Python 3 (python3 / python / py -3)
* A LaTeX engine:

### Ubuntu / WSL

```
sudo apt-get update
sudo apt-get install -y \
  texlive-xetex texlive-latex-extra latexmk fonts-roboto
```

### macOS (Homebrew)

```
brew install --cask mactex
```

### Windows

* Recommended: **WSL** (Ubuntu) → install using commands above.
* Or install **MiKTeX** and ensure `xelatex` is on PATH.

---

## 🚀 GitHub Actions

CI automatically:

1. Installs TeX
2. Generates `main.tex`
3. Builds the PDF
4. Deploys to `public/` for GitHub Pages

So you **never** need to commit the generated PDF manually.

---

## ✔️ Summary

* Edit `contents.json` → updates your resume content
* Edit `layout.tex` → updates the style
* Run `build.sh` → generates & compiles everything
* `main.tex` → generated; never modify manually

This directory is the core of your fully automated resume system.
